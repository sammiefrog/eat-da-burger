<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}FOOD TIME!{{else}}EAT UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
